# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore the default SQLite database.
/db/*.sqlite3
/db/*.sqlite3-*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore uploaded files in development.
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets
/public/vite/assets/

# Ignore master key for decrypting credentials and more.
/config/master.key

/app/assets/builds/*
!/app/assets/builds/.keep

/node_modules

# Terraform関連
# Terraformステートファイル（機密情報を含む可能性があるため）
*.tfstate
*.tfstate.*
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.local.backup

# Terraform設定ファイル（環境固有の値を含む）
*.tfvars
!terraform.tfvars.example

# Terraformプロバイダーとモジュールキャッシュ
.terraform/
.terraform.lock.hcl

# Terraformプランファイル
*.tfplan

# バックアップファイル
*.bak
*.tmp

# SSH秘密鍵（自動生成される）
terraform/ssh-keys/*-key
terraform/ssh-keys/*.pub
terraform/ssh-keys/id_*
!ssh-keys/.keep
!ssh-keys/README.md

# 環境変数ファイル
.env
.env.local
.env.development
.env.test
.env.production
.env.staging

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEファイル
.vscode/
.idea/
*.swp
*.swo
*~

# ログファイル
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Kamal関連（デプロイツール）
.kamal/secrets

# その他の一時ファイル
*.pid
*.seed
*.pid.lock
