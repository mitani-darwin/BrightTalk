# Exclude VCS and metadata
.git
.gitignore
.gitattributes
.DS_Store

# Local editor/IDE settings
.idea
.vscode

# Logs and temporary files
log
log/*
tmp
tmp/*

# Rails storage (ActiveStorage local dir)
storage
storage/*

# Node and Ruby artifacts not needed in build context
node_modules
vendor/bundle
.bundle
coverage

# Test and development files not needed for production image
spec
test
db/*.sqlite3
db/*.sqlite3-journal
!db/migrate  # マイグレーションファイルは含める


# Terraform and infrastructure code (keeps image context small)
terraform

# Compiled assets from previous builds (will be recompiled in image)
public/assets

# Environment and secrets (should be provided via CI/CD or runtime env)
.env
.env.*
config/master.key
config/credentials.yml.enc

# Sample and large media files
db/sample_images

# Other misc
*.swp
*.swo
