<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to "BrightTalk", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to "ホーム", root_path, class: "nav-link #{'active' if current_page?(root_path)}" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "投稿作成", new_post_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "下書き", drafts_posts_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <!-- 通知エリア -->
          <li class="nav-item">
            <a class="nav-link" href="#" title="通知">
              <i class="fas fa-bell"></i>
              <span class="badge bg-danger">0</span>
            </a>
          </li>

          <!-- ユーザーメニュー -->
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <% if current_user.avatar_or_default %>
                <%= image_tag current_user.avatar_or_default, class: "rounded-circle me-2", style: "width: 32px; height: 32px; object-fit: cover;" %>
              <% else %>
                <div class="rounded-circle bg-secondary d-flex align-items-center justify-content-center me-2" style="width: 32px; height: 32px;">
                  <i class="fas fa-user text-white" style="font-size: 14px;"></i>
                </div>
              <% end %>
              <%= current_user.name %>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "マイページ", user_path(current_user), class: "dropdown-item" %></li>
              <li><%= link_to "アカウント設定", user_account_path, class: "dropdown-item" %></li>
              <li><%= link_to "パスキー認証", user_passkeys_index_path, class: "dropdown-item" %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to "ログアウト", destroy_user_session_path, data: { "turbo-method": :delete }, class: "dropdown-item" %></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "ログイン", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "新規登録", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>