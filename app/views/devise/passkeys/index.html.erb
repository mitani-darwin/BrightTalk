<div class="container">
  <h1>パスキー管理</h1>

  <div class="row">
    <div class="col-md-8">
      <% if @passkeys.any? %>
        <div class="card">
          <div class="card-header">
            <h5>登録済みのパスキー</h5>
          </div>
          <div class="card-body">
            <% @passkeys.each do |passkey| %>
              <div class="passkey-item mb-3 p-3 border rounded">
                <div class="d-flex justify-content-between align-items-center">
                  <div>
                    <h6><%= passkey.display_name %></h6>
                    <small class="text-muted">
                      登録日: <%= l(passkey.created_at, format: :short) %>
                    </small>
                  </div>
                  <div>
                    <%= link_to "削除",
                        passkey_path(passkey),
                        method: :delete,
                        confirm: "このパスキーを削除しますか？",
                        class: "btn btn-sm btn-outline-danger" %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      <% else %>
        <div class="alert alert-info">
          <h5>パスキーが登録されていません</h5>
          <p>セキュリティを向上させるため、パスキー認証を設定することをお勧めします。</p>
        </div>
      <% end %>
    </div>

    <div class="col-md-4">
      <div class="card">
        <div class="card-body">
          <h6>新しいパスキーを追加</h6>
          <p class="text-muted">デバイスにパスキーを追加して、よりセキュアにログインできます。</p>
          <%= link_to "パスキーを追加",
                      new_passkey_registration_path,
                      class: "btn btn-primary btn-sm" %>
        </div>
      </div>
    </div>
  </div>
  
  <div class="mt-3">
    <%= link_to "アカウント設定に戻る", account_user_path(current_user), class: "btn btn-secondary" %>
  </div>
</div>