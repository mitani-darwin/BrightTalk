<section class="mx-auto w-full max-w-screen-xl px-4 py-8 sm:px-6 lg:px-10">
  <div class="flex flex-wrap items-center justify-between gap-4">
    <div>
      <h1 class="text-lg font-semibold text-slate-900">パスキー管理</h1>
      <p class="mt-1 text-sm text-slate-500">登録済みパスキーと追加操作をまとめて管理します。</p>
    </div>
    <%= link_to "アカウント設定に戻る", account_user_path(current_user), class: "inline-flex items-center rounded-xl border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-700 transition hover:border-slate-300 hover:text-slate-900" %>
  </div>

  <div class="mt-6 grid gap-6 lg:grid-cols-12">
    <div class="lg:col-span-8">
      <div class="rounded-2xl border border-slate-200 bg-white shadow-sm">
        <div class="border-b border-slate-200 px-6 py-4">
          <h2 class="text-base font-semibold text-slate-900">登録済みのパスキー</h2>
        </div>
        <div class="px-6 py-6">
          <% if @passkeys.any? %>
            <div class="space-y-4">
              <% @passkeys.each do |passkey| %>
                <div class="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-slate-200 bg-white px-4 py-3">
                  <div>
                    <h3 class="text-sm font-semibold text-slate-900"><%= passkey.display_name %></h3>
                    <p class="mt-1 text-xs text-slate-500">登録日: <%= l(passkey.created_at, format: :short) %></p>
                  </div>
                  <%= link_to "削除",
                              passkey_path(passkey),
                              method: :delete,
                              confirm: "このパスキーを削除しますか？",
                              class: "inline-flex items-center rounded-xl border border-rose-200 px-3 py-1.5 text-xs font-semibold text-rose-600 transition hover:bg-rose-50" %>
                </div>
              <% end %>
            </div>
          <% else %>
            <div class="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-4 text-sm text-slate-600">
              <h3 class="text-sm font-semibold text-slate-900">パスキーが登録されていません</h3>
              <p class="mt-2 text-sm text-slate-500">セキュリティを向上させるため、パスキー認証を設定することをお勧めします。</p>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <div class="lg:col-span-4">
      <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <h3 class="text-sm font-semibold text-slate-900">新しいパスキーを追加</h3>
        <p class="mt-2 text-sm text-slate-500">デバイスにパスキーを追加して、よりセキュアにログインできます。</p>
        <%= link_to "パスキーを追加",
                    new_passkey_path,
                    class: "mt-4 inline-flex items-center rounded-xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white shadow-sm transition hover:bg-slate-800" %>
      </div>
    </div>
  </div>
</section>
