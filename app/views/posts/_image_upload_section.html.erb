<% input_base = "mt-2 block w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700 shadow-sm focus:border-slate-300 focus:ring-2 focus:ring-slate-200" %>

<%= form.label :images, "画像", class: "text-sm font-medium text-slate-700" %>
<%= form.file_field :images, multiple: true, accept: "image/*",
                    class: input_base, id: "imageInput" %>
<p class="mt-2 text-xs text-slate-500">
  複数の画像を選択できます（PNG, JPG, GIF形式）。選択すると、内容欄のカーソル位置に画像のマークダウンを自動で挿入します。
</p>

<div id="selectedFilesDisplay" class="mt-3 hidden">
  <div class="text-xs font-semibold text-slate-500">選択されたファイル:</div>
  <div id="selectedFilesList" class="mt-2 rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-600"></div>
</div>

<% if post.persisted? && post.images.attached? %>
  <div class="mt-4">
    <div class="text-xs font-semibold text-slate-500">現在アップロード済みの画像:</div>
    <div class="mt-3 grid grid-cols-2 gap-3 sm:grid-cols-3">
      <% post.images.each do |image| %>
        <div id="image-card-<%= image.id %>" class="rounded-2xl border border-slate-200 bg-white shadow-sm">
          <%= image_tag image, class: "h-24 w-full rounded-t-2xl object-cover" %>
          <div class="p-3">
            <div class="text-xs text-slate-500 truncate"><%= image.filename %></div>
            <div class="mt-2 flex gap-2">
              <button type="button" class="inline-flex flex-1 items-center justify-center rounded-xl border border-slate-200 px-2 py-1 text-xs font-semibold text-slate-700 transition hover:border-slate-300 hover:text-slate-900 insert-existing-image"
                      data-filename="<%= image.filename %>"
                      data-url="attachment:<%= image.filename %>">
                <i class="fas fa-plus mr-1"></i>挿入
              </button>
              <button type="button" class="inline-flex items-center justify-center rounded-xl border border-rose-200 px-2 py-1 text-xs font-semibold text-rose-600 transition hover:bg-rose-50 delete-image-btn"
                      data-post-id="<%= post.id %>"
                      data-attachment-id="<%= image.id %>"
                      data-filename="<%= image.filename %>">
                <i class="fas fa-trash"></i>
              </button>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <p class="mt-3 text-xs text-slate-500">
      <i class="fas fa-info-circle mr-1"></i>
      既存の画像は自動的に保持されます。新しい画像を追加する場合のみ下のファイル選択を使用してください。
    </p>
  </div>
<% end %>
