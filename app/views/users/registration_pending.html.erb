<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-body text-center">
          <div class="mb-4">
            <!-- Font Awesomeが読み込まれない場合に備えてBootstrap Iconsも用意 -->
            <i class="fas fa-envelope fa-3x text-primary"></i>
            <!-- 代替案：Bootstrap Icons -->
            <!-- <i class="bi bi-envelope fs-1 text-primary"></i> -->
          </div>

          <h3 class="card-title mb-3">仮登録が完了しました</h3>

          <p class="card-text">
            <strong><%= @user.email %></strong> 宛に確認メールをお送りしました。
          </p>

          <p class="card-text">
            メール内のリンクをクリックして、アカウント登録を完了してください。
          </p>

          <div class="alert alert-info mt-4">
            <i class="fas fa-info-circle me-2"></i>
            <!-- 代替案：Bootstrap Icons -->
            <!-- <i class="bi bi-info-circle me-2"></i> -->
            <strong>注意:</strong> メールが届かない場合は、迷惑メールフォルダもご確認ください。
          </div>

          <hr class="my-4">

          <div class="d-grid gap-2">
            <!-- 確認メール再送信フォームを修正 -->
            <%= form_with url: user_confirmation_path, method: :post, local: true do |f| %>
              <%= f.hidden_field :user, value: { email: @user.email } %>
              <%= f.submit "確認メールを再送信", class: "btn btn-outline-primary" %>
            <% end %>

            <%= link_to "ホームに戻る", root_path, class: "btn btn-secondary mt-2" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>