<section class="mx-auto w-full max-w-xl px-4 py-10 sm:px-6 lg:px-8">
  <div class="rounded-2xl border border-slate-200 bg-white shadow-sm">
    <div class="px-6 py-8 text-center">
      <div class="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-slate-100 text-slate-600">
        <i class="fas fa-envelope text-2xl"></i>
      </div>

      <h3 class="mt-4 text-lg font-semibold text-slate-900">仮登録が完了しました</h3>

      <p class="mt-3 text-sm text-slate-600">
        <strong class="font-semibold"><%= @user.email %></strong> 宛に確認メールをお送りしました。
      </p>

      <p class="mt-2 text-sm text-slate-500">
        メール内のリンクをクリックして、アカウント登録を完了してください。
      </p>

      <div class="mt-5 rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-600">
        <i class="fas fa-info-circle mr-2 text-slate-400"></i>
        <strong class="font-semibold text-slate-700">注意:</strong> メールが届かない場合は、迷惑メールフォルダもご確認ください。
      </div>

      <div class="mt-6 border-t border-slate-200 pt-6">
        <div class="space-y-3">
          <%= form_with url: user_confirmation_path, method: :post, local: true do |f| %>
            <%= f.hidden_field :user, value: { email: @user.email } %>
            <%= f.submit "確認メールを再送信", class: "inline-flex h-11 w-full items-center justify-center rounded-xl border border-slate-200 bg-white px-4 text-sm font-semibold text-slate-700 shadow-sm transition hover:bg-slate-50" %>
          <% end %>

          <%= link_to "ホームに戻る", root_path, class: "inline-flex h-11 w-full items-center justify-center rounded-xl bg-slate-900 px-4 text-sm font-semibold text-white shadow-sm transition hover:bg-slate-800" %>
        </div>
      </div>
    </div>
  </div>
</section>
