<div class="container mt-4">
  <div class="row">
    <!-- サイドバー -->
    <div class="col-md-3">
      <div class="card">
        <div class="card-header">
          <h5 class="mb-0">アカウント設定</h5>
        </div>
        <div class="list-group list-group-flush">
          <%= link_to user_account_path, class: "list-group-item list-group-item-action" do %>
            <i class="fas fa-user me-2"></i>プロフィール
          <% end %>
          <%= link_to edit_user_account_path, class: "list-group-item list-group-item-action" do %>
            <i class="fas fa-edit me-2"></i>アカウント編集
          <% end %>
          <%= link_to edit_user_password_path, class: "list-group-item list-group-item-action active" do %>
            <i class="fas fa-key me-2"></i>パスワード変更
          <% end %>
          <%= link_to webauthn_credentials_path, class: "list-group-item list-group-item-action" do %>
            <i class="fas fa-fingerprint me-2"></i>WebAuthn認証
          <% end %>
        </div>
      </div>
    </div>

    <!-- メインコンテンツ -->
    <div class="col-md-9">
      <div class="card">
        <div class="card-header">
          <h5 class="mb-0">パスワード変更</h5>
        </div>
        <div class="card-body">
          <div class="alert alert-info">
            <i class="fas fa-info-circle me-2"></i>
            セキュリティのため、現在のパスワードの入力が必要です。
          </div>

          <%= form_with model: @user, url: edit_user_password_path, method: :patch, local: true do |f| %>
            <% if @user.errors.any? %>
              <div class="alert alert-danger" role="alert">
                <h6>パスワード変更でエラーが発生しました：</h6>
                <ul class="mb-0">
                  <% @user.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <div class="mb-3">
              <%= f.label :current_password, "現在のパスワード", class: "form-label" %>
              <%= f.password_field :current_password,
                                   class: "form-control",
                                   required: true,
                                   autocomplete: "current-password" %>
            </div>

            <div class="mb-3">
              <%= f.label :password, "新しいパスワード", class: "form-label" %>
              <%= f.password_field :password,
                                   class: "form-control",
                                   required: true,
                                   autocomplete: "new-password",
                                   minlength: 6 %>
              <div class="form-text">
                6文字以上で設定してください
              </div>
            </div>

            <div class="mb-4">
              <%= f.label :password_confirmation, "新しいパスワード（確認）", class: "form-label" %>
              <%= f.password_field :password_confirmation,
                                   class: "form-control",
                                   required: true,
                                   autocomplete: "new-password" %>
            </div>

            <div class="d-flex gap-2">
              <%= f.submit "パスワードを変更", class: "btn btn-primary" %>
              <%= link_to "キャンセル", user_account_path, class: "btn btn-secondary" %>
            </div>
          <% end %>

          <hr class="my-4">

          <div class="card">
            <div class="card-body">
              <h6><i class="fas fa-shield-alt me-2"></i>より安全な認証方法</h6>
              <p class="mb-3">
                WebAuthn認証（指紋認証、顔認証、セキュリティキーなど）を設定すると、
                パスワードなしでより安全にログインできます。
              </p>
              <%= link_to "WebAuthn認証を設定", new_webauthn_credential_path, class: "btn btn-success btn-sm" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>