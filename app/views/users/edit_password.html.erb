
<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card">
        <div class="card-header">
          <h4>パスワード変更</h4>
        </div>
        <div class="card-body">
          <!-- 正しいパスヘルパーを使用 -->
          <%= form_with model: @user, url: update_user_password_path, method: :patch, local: true do |f| %>

            <!-- WebAuthnユーザーでも通常のパスワード変更を許可 -->
            <!-- 既存パスワードがない場合（初回設定）のみ現在のパスワード入力をスキップ -->
            <% unless current_user.encrypted_password.blank? %>
              <div class="mb-3">
                <%= f.label :current_password, "現在のパスワード", class: "form-label" %>
                <%= f.password_field :current_password, class: "form-control", required: true %>
                <% if current_user.has_webauthn_credentials? %>
                  <div class="form-text text-info">
                    <i class="fas fa-info-circle me-1"></i>
                    WebAuthn認証が設定されていますが、パスワード変更には現在のパスワードが必要です。
                  </div>
                <% end %>
              </div>
            <% else %>
              <!-- 初回パスワード設定の場合の説明 -->
              <div class="alert alert-info mb-3">
                <i class="fas fa-info-circle me-2"></i>
                初回パスワード設定のため、現在のパスワードの入力は不要です。
              </div>
            <% end %>

            <!-- エラーメッセージ表示 -->
            <% if @user&.errors&.any? %>
              <div class="alert alert-danger">
                <% @user.errors.full_messages.each do |message| %>
                  <div><%= message %></div>
                <% end %>
              </div>
            <% end %>

            <div class="mb-3">
              <%= f.label :password, "新しいパスワード", class: "form-label" %>
              <%= f.password_field :password, class: "form-control", required: true %>
              <div class="form-text">最低8文字以上で入力してください</div>
            </div>

            <div class="mb-3">
              <%= f.label :password_confirmation, "新しいパスワード（確認）", class: "form-label" %>
              <%= f.password_field :password_confirmation, class: "form-control", required: true %>
            </div>

            <hr>

            <!-- WebAuthn認証設定 -->
            <div class="mb-3">
              <h5>認証設定</h5>
              <div class="form-check">
                <%= f.check_box :webauthn_enabled, class: "form-check-input" %>
                <%= f.label :webauthn_enabled, "WebAuthn認証を有効にする", class: "form-check-label" %>
              </div>
              <div class="form-text">
                WebAuthn認証を有効にすると、生体認証やセキュリティキーでのログインが可能になります。<br>
                無効にした場合は、メールアドレスとパスワードでのログインになります。
              </div>

              <!-- WebAuthn認証が有効な場合の追加情報 -->
              <% if current_user.webauthn_enabled? %>
                <div class="alert alert-success mt-2">
                  <i class="fas fa-fingerprint me-2"></i>
                  WebAuthn認証が有効です。パスワードと併用してセキュリティを強化できます。
                </div>
              <% end %>
            </div>

            <div class="d-grid gap-2">
              <%= f.submit "変更を保存", class: "btn btn-primary" %>
            </div>
          <% end %>

          <div class="mt-3 text-center">
            <%= link_to "戻る", user_account_path, class: "btn btn-outline-secondary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>