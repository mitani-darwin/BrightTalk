<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-10">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-shield-check me-2"></i>WebAuthn認証管理</h2>
        <%= link_to "新しい認証を追加", new_webauthn_credential_path, class: "btn btn-primary" %>
      </div>

      <% if @webauthn_credentials.any? %>
        <div class="card">
          <div class="card-header">
            <h5 class="mb-0">登録済み認証デバイス</h5>
          </div>
          <div class="card-body">
            <div class="row">
              <% @webauthn_credentials.each do |credential| %>
                <div class="col-md-6 mb-3">
                  <div class="card h-100">
                    <div class="card-body">
                      <div class="d-flex justify-content-between align-items-start">
                        <div>
                          <h6 class="card-title">
                            <i class="bi bi-fingerprint me-2 text-primary"></i>
                            <%= credential.nickname %>
                          </h6>
                          <small class="text-muted">
                            登録日: <%= credential.created_at.strftime("%Y年%m月%d日") %>
                          </small>
                          <br>
                          <% if credential.last_used_at %>
                            <small class="text-success">
                              最終使用: <%= time_ago_in_words(credential.last_used_at) %>前
                            </small>
                          <% else %>
                            <small class="text-warning">未使用</small>
                          <% end %>
                        </div>
                        <div class="dropdown">
                          <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                            <i class="bi bi-three-dots"></i>
                          </button>
                          <ul class="dropdown-menu">
                            <li>
                              <%= link_to webauthn_credential_path(credential),
                                          method: :delete,
                                          data: {
                                            confirm: "本当に削除しますか？",
                                            turbo_method: :delete
                                          },
                                          class: "dropdown-item text-danger" do %>
                                <i class="bi bi-trash me-2"></i>削除
                              <% end %>
                            </li>
                          </ul>
                        </div>
                      </div>
                      <div class="mt-2">
                        <span class="badge bg-secondary">使用回数: <%= credential.sign_count %></span>
                      </div>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        </div>

        <div class="alert alert-info mt-4">
          <h6><i class="bi bi-info-circle me-2"></i>WebAuthn認証について</h6>
          <ul class="mb-0">
            <li>登録されたデバイスを使用してパスワードなしでログインできます</li>
            <li>複数のデバイスを登録することができます</li>
            <li>セキュリティキー、指紋認証、顔認証などが利用可能です</li>
            <li>デバイスを紛失した場合は速やかに削除してください</li>
          </ul>
        </div>

      <% else %>
        <div class="text-center py-5">
          <div class="mb-4">
            <i class="bi bi-shield-x display-1 text-muted"></i>
          </div>
          <h4 class="text-muted">WebAuthn認証が設定されていません</h4>
          <p class="text-muted mb-4">
            より安全で便利なログインのために、WebAuthn認証を設定することをお勧めします。
          </p>
          <%= link_to "WebAuthn認証を設定する", new_webauthn_credential_path, class: "btn btn-primary btn-lg" %>
        </div>
      <% end %>

      <div class="mt-4">
        <%= link_to "戻る", root_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>