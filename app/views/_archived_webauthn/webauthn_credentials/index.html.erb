<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-10">
      <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
          <h4><i class="fas fa-shield-alt me-2"></i>パスキー認証管理</h4>
          <a href="<%= new_passkey_path %>" class="btn btn-primary" data-turbo="false">
            <i class="fas fa-plus me-2"></i>新しい認証を追加
          </a>
        </div>
        <div class="card-body">
          <% if @webauthn_credentials.any? %>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                <tr>
                  <th>認証名</th>
                  <th>登録日時</th>
                  <th>最終使用</th>
                  <th>操作</th>
                </tr>
                </thead>
                <tbody>
                <% @webauthn_credentials.each do |credential| %>
                  <tr>
                    <td>
                      <i class="fas fa-fingerprint me-2"></i>
                      <%= link_to credential.name, webauthn_credential_path(credential),
                                  class: "text-decoration-none" %>
                    </td>
                    <td><%= credential.created_at.strftime('%Y年%m月%d日 %H:%M') %></td>
                    <td>
                      <% if credential.last_used_at %>
                        <%= credential.last_used_at.strftime('%Y年%m月%d日 %H:%M') %>
                      <% else %>
                        <span class="text-muted">未使用</span>
                      <% end %>
                    </td>
                    <td>
                      <div class="btn-group" role="group">
                        <%= link_to webauthn_credential_path(credential),
                                    class: "btn btn-sm btn-outline-primary" do %>
                          <i class="fas fa-eye me-1"></i>詳細
                        <% end %>
                        <%= button_to webauthn_credential_path(credential),
                                      method: :delete,
                                      form: {
                                        "data-turbo-confirm": "この認証を削除してもよろしいですか？"
                                      },
                                      class: "btn btn-sm btn-outline-danger" do %>
                          <i class="fas fa-trash me-1"></i>削除
                        <% end %>
                      </div>
                    </td>
                  </tr>
                <% end %>
                </tbody>
              </table>
            </div>
          <% else %>
            <div class="text-center py-5">
              <i class="fas fa-fingerprint fa-3x text-muted mb-3"></i>
              <h5 class="text-muted">パスキー認証が登録されていません</h5>
              <p class="text-muted mb-3">Touch ID、Face ID、またはセキュリティキーを追加して、より安全で便利なログインを開始しましょう。</p>
              <a href="<%= new_passkey_path %>" class="btn btn-primary" data-turbo="false">
                <i class="fas fa-plus me-2"></i>最初の認証を追加
              </a>
            </div>
          <% end %>
        </div>
        <div class="card-footer">
          <a href="<%= user_account_path %>" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-2"></i>アカウント設定に戻る
          </a>
        </div>
      </div>
    </div>
  </div>
</div>